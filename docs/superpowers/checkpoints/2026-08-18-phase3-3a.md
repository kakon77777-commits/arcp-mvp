# Phase 3A Checkpoint — Temporal Evidence + CTCL v1 Adapter

Status: COMPLETE

- Provider-neutral `@arcp/temporal-evidence` contract added.
- Explicit `local:unverified:*` degraded evidence added; degraded fallback never forges a CTCL id.
- `InstantRef` received only the backwards-compatible optional widening required to persist real 3A evidence (`posix`, additional encodings, value, synchronized quality, attestation).
- `CtclRestTemporalAdapter` added behind injected Fetch with strict v1 envelope parsing, normalized provider/HTTP errors, HTTPS/custom-base guard, and no OAuth dependency.
- CTCL `/v1/now`, register/get instant, convert, boundary inspect, and shared-instant planner are fixture-tested without live network access.
- Provider precision is preserved; a `unix_ns` representation does not upgrade `millisecond_representation` source precision.
- `DeterministicTemporalEvidenceAdapter` added for async provider-neutral tests.
- Legacy synchronous `FakeCtclAdapter` remains exported for Phase 0 compatibility.
- Temporary lockfile workflow removed after pnpm 11.8.0 regeneration.

Verification at branch checkpoint `98e731a9f07c60812151c151c0732a91b38a09f9`:

- frozen install: PASS across 13 workspace projects
- CTCL HTTP adapter: 17/17 PASS
- temporal evidence + deterministic provider: 5/5 PASS
- full suite: 29/29 test files, 203/203 tests PASS
- TypeScript typecheck: PASS
- normal CI remains credential-free and network-free

Next: Phase 3B — temporal provenance, Ed25519 attestation verification, and degraded-time trust.
